import Layout from '../components/Layout';

import React from 'react';

type Props = {};

const Home = (props: Props) => {
  return (
    <Layout>
      <h1>Home</h1>
    </Layout>
  );
};

export default Home;
